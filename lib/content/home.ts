/**
 * Тексты главной. Правило 2: ни одной фразы в JSX — правка заголовка
 * не должна требовать чтения компонента. Сейчас здесь один каркас:
 * секции и их тексты добавляет автор сайта (`/new-section`).
 */
export const HOME = {
  lead: 'Оставить заявку',
} as const

---
name: seo-check
description: Проверить сайт перед публикацией — метадата, robots, sitemap, разметка, заглушки. Использовать перед деплоем или по запросу «проверь SEO», «всё ли готово к запуску».
---

# Проверка перед публикацией

Пройди по списку и отчитайся по каждому пункту — что в порядке, что нет.

1. `pnpm test` — guard заглушек зелёный?
2. `pnpm lint` — типы чистые?
3. `pnpm build` — сборка проходит?
4. `SITE.indexable`. Смотри, когда именно ты это гоняешь:
   - **перед публикацией**, шагом 1 внутри `/deploy`, — `'private'`
     здесь правильное значение, режим переключит сам `/deploy` шагом 2.
     Просто назови, что стоит сейчас и что будет стоять после;
   - **на уже опубликованном сайте** — `'public'` для обычного сайта,
     `'ads-only'` для лендинга под рекламу, а `'private'` почти всегда
     ошибка: сайт закрыт от поиска и никто этого не заметил.
5. `curl -s <адрес>/robots.txt` — соответствует режиму? Адрес — тот,
   что напечатал `pnpm dev`: при занятом порте Next уходит на следующий.
6. `curl -s <адрес>/sitemap.xml` — в нём каждая **индексируемая**
   страница? Страницам с `robots: { index: false }` (политика, оферта,
   `/spasibo`) в sitemap не место: Search Console ответит на них ошибкой
   «Отправленный URL содержит тег noindex».
7. У каждой страницы уникальные `title` и `description`? Найди дубли.
8. `title` до 60 символов, `description` 120–160?
9. Ровно один `<h1>` на страницу?
10. У всех `next/image` заполнен `alt`?
11. JSON-LD валиден? Проверь, что нет `aggregateRating` без источника.
    Если на странице есть `<details>` с вопросами — присутствует ли
    соответствующий `FAQPage`, и совпадают ли ответы в разметке
    с видимым текстом?
12. `curl -s <адрес>/opengraph-image -o /tmp/og.png` — кириллица
    отрисовалась, а не квадраты?
13. Нет ли `new Date()` в `sitemap.lastModified`?
14. Заполнены ли реквизиты на юридических страницах?
